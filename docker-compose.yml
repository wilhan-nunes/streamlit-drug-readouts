version: '3'
services:
  streamlit-drug-readouts:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: streamlit-drug-readouts
    volumes:
      - ./logs:/app/logs:rw
      - ./input_tables:/app/input_tables:rw
    networks:
      - default
      - nginx-net
    restart: unless-stopped
    command: /app/run_server.sh
    logging:
      driver: json-file
      options:
        max-size: "10m"      # Maximum size of a single log file
        max-file: "3"        # Maximum number of log files to keep (rotates after 3 files)
    deploy:
      resources:
        limits:
          memory: 8000M
    environment:
      VIRTUAL_HOST: drugreadouts.gnps2.org
      VIRTUAL_PORT: 5000
      LETSENCRYPT_HOST: drugreadouts.gnps2.org
      LETSENCRYPT_EMAIL: mwang87@gmail.com

  streamlit-drug-readouts-redis:
    image: redis:7.2
    container_name: streamlit-drug-readouts-redis
    networks:
      - default
    restart: unless-stopped
    volumes:
      - ./redis-data:/data
    command: ["redis-server", "--save", "60", "1", "--loglevel", "warning", "--maxmemory", "2gb", "--maxmemory-policy", "allkeys-lru"]

  streamlit-drug-readouts-worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: streamlit-drug-readouts-worker
    command: /app/run_worker.sh
    volumes:
      - ./logs:/app/logs:rw
      - ./input_tables:/app/input_tables:rw
    depends_on:
      - streamlit-drug-readouts-redis
    networks:
      - default
    restart: unless-stopped

networks:
  nginx-net:
    external:
      name: nginx-net